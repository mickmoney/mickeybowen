---
import { sanityClient, urlFor } from '../lib/sanityClient';
import { APPLY_PAGE_QUERY, SITE_SETTINGS_QUERY } from '../lib/queries';

const page = await sanityClient.fetch(APPLY_PAGE_QUERY);
const settings = await sanityClient.fetch(SITE_SETTINGS_QUERY);

const navLinks = settings?.navLinks?.length
  ? settings.navLinks
  : [
      { label: 'Work', anchor: '/#work' },
      { label: 'Process', anchor: '/#process' },
      { label: 'Pricing', anchor: '/pricing' },
      { label: 'About', anchor: '/#about' },
      { label: 'Blog', anchor: '/blog' },
    ];
const navCta = settings?.navCta ?? {
  label: 'Chat via WhatsApp',
  url: 'https://wa.me/5016082944',
  openInNewTab: true,
};
const footerText = settings?.footerText ?? '© 2026 Mickey Bowen';

const rating = page?.socialProofRating ?? 4.5;
const fullStars = Math.round(rating);
const proofText = page?.socialProofText ?? 'based on 30+ client reviews';
const avatars = (page?.testimonialsTop ?? []).map((t) => t.photo).filter(Boolean);
---
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Apply | {settings?.siteName ?? 'Mickey Bowen'}</title>
  <meta name="description" content="Tell me a bit about your team so I can see if a landing page sprint is the right fit." />
</head>
<body>
  <div class="nav-bar">
    <nav>
      <a href="/" class="logo">
        <span class="nav-avatar">
          <img
            src={settings?.navAvatar ? urlFor(settings.navAvatar).width(72).height(72).url() : '/images/mickey-headshot.jpg'}
            alt={settings?.siteName ?? 'Mickey Bowen'}
          />
        </span>
        {settings?.siteName ?? 'Mickey Bowen'}
      </a>
      <div class="links">
        {navLinks.map((link) => (
          <a href={link.anchor}>{link.label}</a>
        ))}
      </div>
      <div class="nav-cta">
        <a
          href={navCta.url}
          target={navCta.openInNewTab ? '_blank' : undefined}
          rel={navCta.openInNewTab ? 'noopener' : undefined}
          class="btn btn-whatsapp nav-btn"
        >
          <svg width="15" height="15" viewBox="0 0 24 24" fill="currentColor"><path d="M12.04 2C6.58 2 2.13 6.45 2.13 11.91c0 1.75.46 3.48 1.32 5L2 22l5.25-1.38c1.45.79 3.08 1.21 4.79 1.21h.01c5.46 0 9.91-4.45 9.91-9.91C22 6.45 17.5 2 12.04 2zm5.72 14.02c-.24.68-1.4 1.32-1.93 1.4-.49.08-1.11.11-1.79-.11-.41-.13-.94-.31-1.62-.6-2.85-1.23-4.71-4.09-4.85-4.28-.14-.19-1.16-1.54-1.16-2.94s.73-2.09.99-2.38c.26-.29.57-.36.76-.36.19 0 .38 0 .55.01.18.01.41-.07.64.49.24.58.81 2 .88 2.15.07.15.12.32.02.51-.09.19-.14.31-.28.48-.14.17-.29.37-.42.5-.14.14-.28.29-.12.57.16.28.71 1.17 1.52 1.9 1.04.93 1.92 1.22 2.2 1.36.28.14.44.12.6-.07.17-.19.71-.83.9-1.11.19-.28.38-.24.64-.14.26.09 1.66.78 1.94.92.28.14.47.21.54.33.07.12.07.68-.17 1.36z"/></svg>
          {navCta.label}
        </a>
      </div>
    </nav>
  </div>

  <main>
    <div class="apply-wrap">
      <div class="form-card">

        <div class="trust-row">
          {avatars.length > 0 && (
            <div class="avatar-stack">
              {avatars.map((photo) => (
                <span class="mini-avatar">
                  <img src={urlFor(photo).width(64).height(64).url()} alt="" />
                </span>
              ))}
            </div>
          )}
          <div class="trust-copy">
            <div class="stars">
              {Array.from({ length: 5 }).map((_, i) => (
                <svg width="14" height="14" viewBox="0 0 20 20" fill={i < fullStars ? '#2A5BFF' : 'none'} stroke="#2A5BFF" stroke-width="1.3">
                  <path d="M10 1.5l2.6 5.5 6 .7-4.4 4.1 1.2 6-5.4-3-5.4 3 1.2-6L1.4 7.7l6-.7z" />
                </svg>
              ))}
            </div>
            <span>{rating} out of 5, {proofText}</span>
          </div>
        </div>

        <h1>Let's start the conversation.</h1>
        <p class="sub">This isn't a sales pitch with a deck. Answer a few quick questions and I'll follow up personally to see if a sprint is the right fit, no matter what you decide.</p>

        <form>
          <fieldset>
            <legend>Do you run or work for a B2B SaaS company? <span class="req">*</span></legend>
            <label class="option">
              <input type="radio" name="role" value="founder" />
              <span>Yes, I'm the founder / CEO</span>
            </label>
            <label class="option">
              <input type="radio" name="role" value="marketing-leader" />
              <span>Yes, I'm the marketing leader (CMO / Marketing Director)</span>
            </label>
            <label class="option">
              <input type="radio" name="role" value="marketing-team" />
              <span>Yes, I'm on the marketing team</span>
            </label>
            <label class="option">
              <input type="radio" name="role" value="other" />
              <span>Other</span>
            </label>
          </fieldset>

          <a
            href={navCta.url}
            target="_blank"
            rel="noopener"
            class="btn btn-primary next-btn"
          >
            Next
            <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M5 12h14M13 6l6 6-6 6"/></svg>
          </a>
        </form>

        <p class="form-note">Placeholder form - a full intake form is coming soon. Tapping Next reaches Mickey directly on WhatsApp in the meantime.</p>
      </div>
    </div>
  </main>

  <footer>
    <span>{footerText}</span>
  </footer>
</body>
</html>

<style>
  :root {
    --ink: #0B1220;
    --ink-soft: #5B6472;
    --ink-faint: #94A3B8;
    --blue: #2A5BFF;
    --off-white: #FAF9F6;
    --line: #E7EAF0;
    --panel: #F5F7FA;
  }
  * { box-sizing: border-box; }
  body {
    margin: 0;
    padding-top: 72px;
    font-family: 'Inter', -apple-system, BlinkMacSystemFont, sans-serif;
    color: var(--ink);
    background: var(--off-white);
  }
  a { color: inherit; text-decoration: none; }

  .nav-bar {
    position: fixed; top: 0; left: 0; right: 0; z-index: 100;
    background: rgba(255, 255, 255, 0.8);
    -webkit-backdrop-filter: blur(10px); backdrop-filter: blur(10px);
  }
  nav {
    max-width: 1080px; margin: 0 auto; padding: 16px 24px;
    display: flex; align-items: center; justify-content: space-between; gap: 24px;
  }
  .logo { display: flex; align-items: center; gap: 10px; font-weight: 700; font-size: 16px; }
  .nav-avatar {
    width: 32px; height: 32px; border-radius: 50%; overflow: hidden; flex-shrink: 0;
    background: var(--off-white); display: flex; align-items: center; justify-content: center;
    box-shadow: inset 0 0 0 2px #FFFFFF;
  }
  .nav-avatar img { width: 100%; height: 100%; object-fit: cover; }
  .links { display: flex; gap: 28px; font-size: 14px; color: var(--ink-soft); }
  .links a:hover { color: var(--ink); }
  .btn { display: inline-flex; align-items: center; gap: 8px; padding: 10px 20px; border-radius: 999px; font-weight: 600; font-size: 14px; border: none; cursor: pointer; font-family: inherit; }
  .btn-primary { background: var(--blue); color: #fff; }
  .btn-whatsapp { background: #fff; color: var(--ink); border: 1.5px solid rgba(11, 18, 32, 0.2); }
  .btn-whatsapp svg { flex-shrink: 0; color: #25D366; }

  main { padding: 56px 24px 88px; }
  .apply-wrap { max-width: 640px; margin: 0 auto; }

  .form-card {
    background: #FFFFFF; border: 1px solid var(--line); border-radius: 20px;
    padding: 40px clamp(24px, 5vw, 48px); box-shadow: 0 24px 54px rgba(15, 23, 42, 0.06);
  }

  .trust-row { display: flex; align-items: center; gap: 14px; margin: 0 0 24px; flex-wrap: wrap; }
  .avatar-stack { display: flex; }
  .mini-avatar {
    width: 34px; height: 34px; border-radius: 50%; overflow: hidden; flex-shrink: 0;
    box-shadow: 0 0 0 2px #FFFFFF; margin-left: -10px; background: var(--panel);
  }
  .mini-avatar:first-child { margin-left: 0; }
  .mini-avatar img { width: 100%; height: 100%; object-fit: cover; }
  .trust-copy { display: flex; flex-direction: column; gap: 3px; }
  .stars { display: flex; gap: 2px; }
  .trust-copy span { font-size: 12.5px; color: var(--ink-soft); font-weight: 500; }

  h1 { font-size: clamp(24px, 3.2vw, 30px); line-height: 1.2; margin: 0 0 12px; letter-spacing: -0.015em; }
  p.sub { font-size: 15px; line-height: 1.6; color: var(--ink-soft); margin: 0 0 32px; }

  fieldset { border: none; padding: 0; margin: 0 0 24px; }
  legend { font-size: 14.5px; font-weight: 600; margin: 0 0 14px; padding: 0; }
  .req { color: var(--blue); }
  .option {
    display: flex; align-items: center; gap: 12px;
    border: 1.5px solid var(--line); border-radius: 12px; padding: 14px 16px;
    margin-bottom: 10px; cursor: pointer; transition: border-color 0.15s ease, background 0.15s ease;
  }
  .option:hover { border-color: var(--blue); background: rgba(42, 91, 255, 0.03); }
  .option input { accent-color: var(--blue); width: 16px; height: 16px; flex-shrink: 0; }
  .option:has(input:checked) { border-color: var(--blue); background: rgba(42, 91, 255, 0.05); }
  .option span { font-size: 14.5px; font-weight: 500; }

  .next-btn { width: 100%; justify-content: center; height: 50px; font-size: 15px; }
  .form-note { font-size: 12.5px; color: var(--ink-faint); text-align: center; margin: 18px 0 0; }

  footer {
    padding: 28px 24px; border-top: 1px solid var(--line);
    color: var(--ink-faint); font-size: 13px; text-align: center;
  }

  @media (max-width: 640px) {
    nav { flex-wrap: wrap; }
    .links { display: none; }
    .form-card { padding: 32px 20px; border-radius: 16px; }
  }
</style>
